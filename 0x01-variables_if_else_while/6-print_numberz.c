#include <stdio.h>

/**
 * main - program that prints all single digits of base 10 starting from 0
 *
 * Return: 0
 */

int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
		putchar(a);

	putchar(10);
	return (0);
}
