#include <stdio.h>

/**
 * main - program that prints all single digits of base 10 starting from 0
 *
 * Return: 0
 */

int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
